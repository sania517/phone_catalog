export enum sortOptions {
  name = 'name',
  newest = 'newest',
  oldest = 'oldest',
  cheaper = 'cheaper',
  expensive = 'expensive',
  rating = 'rating',
}

export enum goodsOptions {
  phone = 'phone',
  tablet = 'tablet',
  accessory = 'accessory',
}

export enum paginationOptions {
  'l16' = '16',
  'l25' = '25',
  'l50' = '50',
  'l100' = '100',
}
