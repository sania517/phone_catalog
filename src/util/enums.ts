export enum sortOptions {
  name = 'name',
  newest = 'newest',
  oldest = 'oldest',
  cheaper = 'cheaper',
  expensive = 'expensive',
  rating = 'rating',
}

export enum goodsOptions {
  phone = 'phone',
  tablet = 'tablet',
  accessory = 'accessory',
}
