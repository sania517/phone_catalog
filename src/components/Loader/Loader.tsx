import React, { FC } from 'react';
import './Loader.scss';

export const Loader: FC = () => (
  <div className="loader__container">
    <h2 className="loader__title">Loding data .... </h2>
  </div>
);
